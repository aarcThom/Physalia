---
name: iterative-canvas-editing
description: "Canvas-state grounding + ghpatch dual-mode CompTx (GhJSON 1.1.1) — LLM sees live canvas, edits in place; fixes alpha tester's reopen-duplication; built 2026-07-07, live Rhino test pending"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3b09d213-7ddf-48be-8581-8780d294732c
---

**Iterative canvas editing landed 2026-07-07** (working tree; builds clean, 222 Core tests green, live Rhino test PENDING). Plan: `C:\Users\rober\.claude\plans\i-had-an-alpha-noble-flurry.md`. Motivation: alpha tester's duplicated components (CompTx's in-memory `_placedGuids` forgets across reopen → orphan + duplicate) and no LLM canvas visibility.

Architecture — canvas is the source of truth; no session ID map:
- **GhJSON NuGet 1.0.0 → 1.1.1** (both csprojs). 1.1.1 gives doc-level GhPatch (`GhJson.ApplyPatch/PatchFromJson/ValidatePatch/Diff`) + public canvas primitives (`Delete/Connect/Disconnect/CaptureExternalConnections/FindObject`). BREAKING: `GhJsonPivot.X/Y` became **int** (was float) — call sites now round. See [[ghjson-library-reference-only]] (no library edits; known add-id renumber bug pre-remapped around in Physalia).
- **`GhJsonBridge` is now partial**: `GhJsonBridge.CanvasState.cs` (`TryExportCanvasState` — filters `doc.Objects` by `assembly != typeof(PhyBase).Assembly`, `GetByGuids`, compact JSON + own `sha256-` checksum of the exact text; SINGLE shared frame for grounder + apply) and `GhJsonBridge.Patch.cs` (`IsPatchJson`, `ApplyPatchToCanvas` → `CanvasPatchOutcome`). Apply flow: parse → re-export base → drift-check carried checksum (strict default) → pre-pass adds (StampComponentGuids; explicit-only `physalia.reference` resolution — nickname-implied rule deliberately OFF in patch mode; collision id pre-remap incl. endpoint/group rewrites; pivot fallback) → doc-level `GhJson.ApplyPatch(VerifyBase=false, RenumberCollidingAddedIds=false)` → canvas reconciliation: fast-path SDK mutation (slider value/panel text/nickname/pivot/locked/hidden) else capture-wires→Delete→re-`Put` with `RegenerateInstanceGuids=false` (same guid survives)→rewire; adds via one `Put` + paramIndex-first wiring (`FindEndpointParam preferIndex:true`); groups via GH SDK.
- **`CanvasStateGrounder`** (Components/Grounding, GUID A4C8E7D2-…): CanvasInputGrounder pattern + 300ms debounce w/ trailing re-check + `IsImporting` skip; checksum = change signature. New Core `CanvasStateGrounding` record renders JSON + copy-checksum-verbatim instruction; empty canvas → empty section. ConversationLog untouched (default switch case passes it through).
- **CompTx dual mode**: `GhPatchDetector.IsGhPatch` (Core/Validation kind sniff) routes; patch branch never calls `RemovePreviouslyPlaced` and clears `_placedGuids` on success; Success payload = added+modified guids (CanvasObservation unchanged); `BuildPatchFeedback` says "listed ops did NOT apply, everything else DID — resubmit only corrections"; right-click "Apply patches on base mismatch (lenient)" persisted as `LenientBase`.
- **Assets**: `PREAMBLE/Node Graph Editing.txt` (+Minified), `SCHEMA/GhPatchSchema.json`, `SCHEMA/GhJSON or GhPatch.json` (oneOf umbrella so ONE Schema Validator gates both; shared $defs). Legacy `Node Graph.txt`/`GhJSONSchema.json` untouched.
- **Tests**: CanvasStateGroundingTests, GhPatchDetectorTests, PromptSchemaAssetTests (validates each schema asset's own examples + umbrella discrimination + pins the 1.1.1 patch API).

**Live-test checklist** (from plan §Verification): slider modified in place (same InstanceGuid), replace-path wire preservation, match_not_found self-correction, checksum-mismatch strict/lenient, save-reopen-edit (the tester's bug), legacy preset regression, slider-drag churn profiling. Known v1 limits: multi-step undo per patch; cluster adds in patches unsupported; export includes internalized data (token cost on heavy canvases; stripped only for Rhino-referenced params).

**Follow-up 2026-07-07 — CanvasInputGrounding RETIRED, Rhino refs detected doc-side** (builds+218 tests clean, live test pending): new `Generation/CanvasRhinoReferences` (`Collect`/`IsRhinoReferenced` — scans floating params for goo with `IsReferencedGeometry`; covers hand-referenced params too) replaces the tool-registry mechanism. Canvas-state export now stamps such params `physalia.rhinoRef` (+ strips their baked internalizedData — the library bakes VALUES, never ReferenceIDs, so a replace would sever the live link → patch modify-guard refuses non-fast-path modifies on them). DELETED: `CanvasInputGrounding` record, `CanvasInputGrounder` component, `RhinoGeometryTool` registry (`_placed`/`PruneMissing`/`CollectReferenceableInputs`/Write-Read; alias dedup via detector), patch `ResolvePatchReferences`. Legacy `ExtractReferences` (full-doc path) now feeds from the detector. Chat page renamed "Referenced Rhino Geometry" (`referencedGeometryWired`/`availableReferencedGeometry` keys — also fixed a latent key mismatch where C# sent `canvasInputs` but Svelte read `availableCanvasInputs`, so the old page was always empty). Preamble/schema rules teach the marker; legacy `Node Graph.txt`/`GhJSONSchema.json` untouched.
