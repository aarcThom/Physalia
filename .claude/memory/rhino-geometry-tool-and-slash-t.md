---
name: rhino-geometry-tool-and-slash-t
description: "Rhino-geometry tool, /t/ tool references, Tools Present moved to Grounding, purple /-command highlighting"
metadata: 
  node_type: memory
  type: project
  originSessionId: ce95986e-bc71-44a8-a88e-40ec522eb3e9
---

> **SUPERSEDED IN PART (2026-07-07)** — the reference-reuse machinery described in Follow-ups 1/3 is RETIRED: `CanvasInputGrounding`/`CanvasInputGrounder`/`CollectReferenceableInputs`/the tool's `_placed` registry are DELETED. Rhino references are now detected doc-side (`Generation/CanvasRhinoReferences` — goo with `IsReferencedGeometry`) and annotated into the canvas-state export as `physalia.rhinoRef`; the chat page is renamed "Referenced Rhino Geometry". See [[iterative-canvas-editing]]. The tool itself (bake+reference+place, arrow-tip placement, alias dedup via the detector) still works as described.

Landed 2026-07-01 on branch `feat/add-rhino-geometry-python-nodes`. Builds clean (`dotnet build src/Physalia.slnx -c Debug`), 141 Core tests green, svelte-check clean. **Live Rhino test pending.** Four connected features:

1. **Rhino Geometry tool** — new `src/Physalia.GH/Components/Tools/RhinoGeometryTool.cs` (`ToolComponentBase`, tool name `create_rhino_geometry`, GUID `7D3F1A94-…`). LLM supplies a geometry spec (point/line/polyline/curve/circle + points/degree/closed/radius/center/name); the tool BAKES a RhinoCommon object into `RhinoDoc.ActiveDoc.Objects` and drops a floating GH param (`Param_Point`/`Param_Curve`) that references it by GUID (`new GH_Curve { ReferenceID = id }` + `LoadGeometry(doc)` → `PersistentData.Append`). Doc mutation deferred to `RhinoApp.Idle` (mirrors ComponentTransmitter) — can't mutate GH/Rhino doc mid-solve. Purpose: give the definition a real Rhino input (e.g. a curve into a Python Script) instead of a slider.

2. **Tools flow reworked: `Instructions` now carries `Tools`.** `Physalia.Core/ConvoInstruct/Instructions.cs` gained `IReadOnlyList<ToolDefinition> Tools { get; init; } = []`. This preserves the [[signal-carrier-discipline]] god-object guard (tools live INSIDE Instructions, which already rides the signal — not a new PhySignal carrier field). LLM Call **dropped its Tools input** and now reads `data.Tools` (its Cancel input shifted from index 2→1). Compaction re-mint sites (`CompactionComponentBase.Succeed`, `Summarizer`) MUST carry `Tools` forward (`{ Tools = source.Tools }`) or tools vanish past a compactor.

3. **Tools Present → Grounding.** `ToolsInUse` (display name changed to "Tools Present") moved from the **Tools** ribbon category to **Grounding**; its output is now `Param_Grounding` emitting `GH_Grounding(new ToolsGrounding(defs))` (new sealed record in Core `Grounding.cs`; `ToSystemPromptSection()` is EMPTY by design — tools reach the model via native tool-calling, not prose; the record is purely a carrier). Wire it into the **Conversation Log's existing Grounding input** (not the LLM Call). Conversation Log extracts `ToolsGrounding` in `ReadGroundingInputs` → `_liveTools`, lifts them onto minted `Instructions.Tools`, and exposes `AvailableToolNames` + `HasToolsGrounding`. Each tool's vestigial "Tool" output stays (harmless) — Tools Present reads `AdvertisedDefinition` directly off the canvas.

**Follow-up 2026-07-01 — CompTx reuses referenced inputs (no duplicates).** Problem: after the geometry tool placed a `Param_Curve`, the LLM's next GhJSON graph recreated a duplicate curve param (unconnected) instead of wiring into the existing one. Fix (chosen over incremental-canvas-modify): let a GhJSON node *reference* an existing on-canvas input, reusing the anchored-placement machinery. Changes: (a) `RhinoGeometryTool` now assigns each placed param a **unique name**, persists a `{guid,name,type}` registry (Write/Read), reports the final name in its tool result, prunes deleted params in `OnSolveTick`, and exposes `static CollectReferenceableInputs(GH_Document) → ReferenceableInput{Name,TypeName,IGH_Param}`. (b) New Core `CanvasInputGrounding(IReadOnlyList<CanvasInput>)` + GH **`CanvasInputGrounder`** ("Canvas Inputs", Grounding category, live-refresh like `ToolsInUse`) — wire into Conversation Log.Grounding; folds through unchanged (no Conversation Log edit). (c) `GhJsonBridge`: new `physalia.reference` extension + `ExtractReferences` (lifts reference nodes out before Put — detects by the `physalia.reference` ext OR a nickName matching a live input; unresolved explicit refs → dropped + reported on Fail) + `RewireReferences` which **reuses `RewireClusterConnections`/`FindEndpointParam`** (a floating `IGH_Param` resolves to itself; graph side by name→paramIndex). Wired into `PlaceDocument` (before `StampComponentGuids`) + `ExecutePut` (new `referencePlan` param). (d) `Node Graph.txt` + `GhJSONSchema.json`: instruct the LLM to reference a listed canvas input by setting a param node's nickName to the exact name (no value/incoming wire). 145 Core tests green, slnx builds clean, **live Rhino test pending**. Plan: `C:\Users\rober\.claude\plans\the-rhino-tool-worked-zany-pearl.md`.

**Follow-up 5 (2026-07-02) — tools grounding now constrains the model in-prompt.** User: "the tool grounding still allows the llm to try tools which are not actively on the canvas." Native tool-calling already limits the model to `instructions.Tools` (on-canvas, router-wired, selection-filtered), and `ToolsGrounding.ToSystemPromptSection` was empty — so the grounding TEXT wasn't telling the model anything. Fix: `ToolsGrounding.ToSystemPromptSection` now lists the tool names + "Call ONLY these tools — never invent or call a tool not in this list." Conversation Log `BuildGroundedSystemPrompt` rewritten (Select→foreach) to collapse all wired ToolsGroundings into ONE section built from `SelectedTools()` (matches what's advertised). 155 Core tests pass; **GH build hit only a `.gha` file lock (Rhino/VS open) — compilation succeeded, close Rhino to refresh bin.** **Root cause turned out to be model hallucination** (DeepSeek kept calling `fetch_url`, which doesn't exist — the real web tool is `read_url`). Native tool-calling means the model can only *validly* call advertised tools, but it can still EMIT an invented tool_use. Two-part fix: (a) the explicit grounding list above (proactive — "call only these"); (b) `ToolDispatchRound.Plan`'s synthetic error for an unmatched call now NAMES the available tools ("The tool \"fetch_url\" does not exist. The available tools are: read_url, web_search. Call one of those…") so the model self-corrects instead of re-guessing — the Router already answers unmatched calls with a synthetic error (so the round never hangs); it was just uninformative. 156 Core tests pass. (The earlier ClaudeCode-built-in-tools theory was wrong for this case.)

**Follow-up 4 (2026-07-02) — reference-marker rework + contenteditable prompt.** Markers changed: clusters `/c/`→**`/cl/`** (`PromptClusterResolver`); `/c/` now means **components** as `/c/<tab>/<component>` (new `PromptComponentResolver`, normalizes to `the "X" component`, tab segment is navigational-only/skipped; 3 markers are distinct so the resolvers compose in any order). `/t/` tools unchanged. Conversation Log exposes `IncludedComponentEntries` (grounded catalog filtered by selection → grouped by Category→component names); ChatWindow pushes `availableComponents` (tab→components; kept OUT of the change-detection signature — large — with a `componentCount` proxy instead) and chains `PromptComponentResolver` in `NormalizeRefs`. bridge `ComponentTabInfo`; App passes `componentTabs`. **System Prompt fully rewritten from transparent-textarea+backdrop to a single `contenteditable` div** — this is what finally allows per-token fonts (slash-commands monospace-on-chip, prose sans) with a CORRECT caret, because the caret now lives in the styled content (the textarea+backdrop scheme could never do per-token fonts without drift). `text` is the source of truth; the editor innerHTML is rendered imperatively from it (`highlightHtml`, `\n`↔`<br>`) with caret restored by char-offset (`caretOffset`/`setCaret` via range-clone + `extractText`; standard contenteditable-highlighting pattern). Enter=submit, Shift+Enter/paste inserted manually (no browser `<div>`/`<br>`), IME via composition flag, placeholder via scoped `.prompt-editor:empty::before`. `.slash-cmd` (app.css) gained the monospace font-family (safe now — contenteditable). Autocomplete is staged: `/`→kind (`/c`,`/cl`,`/t`), `/c/`→tab→component, `/cl/`→cluster, `/t/`→tool; accepting a kind/tab inserts the next marker and re-opens the next stage. 153 Core tests + svelte-check + slnx build clean. **Live Rhino test pending — contenteditable caret/newline/paste/IME are the risk areas.**

**Follow-up 3 (2026-07-01) — UI polish batch + tools selection.** (1) Prompt textarea: `spellcheck={false}` (no red squiggles). (2) Slash commands now monospace on a soft-blue chip: the WHOLE input is `font-mono` (both textarea + backdrop) — required for caret parity since only-command monospace would diverge widths; `.slash-cmd` chip uses `background + border-radius + box-shadow spread` (NEVER padding — metric-affecting). New `--slash-command-bg` token. (3) System Prompt `/` autocomplete is now two-level: typing `/` opens a KIND menu (`/t Tools`, `/c Clusters`) that inserts the `/x/` marker and chains into the existing NAME menu (`refMode: 'kind'|'name'`). (4)+(5)+(6) Tools grounding now has a UI page under Grounding where the user disables individual on-canvas tools: new Core `ToolsSelection` (mirrors `ClusterSelection`), Conversation Log `_toolsSelection`/`SetToolsSelection`/`ToolsSelectionOrNull` + Write/Read + `SelectedTools()` filters `Instructions.Tools` (all present tools still shown in the UI list; only enabled sent to model + used for `/t/`). ChatWindow `settools` dispatch + `HandleSetTools` + pushes `toolsSelection`. **Interpretation of "tools grounding returns ALL tools → only active":** `ToolsInUse` already filters to router-wired tools (dispatchable = active); the real gap was no way to exclude any — the selection provides it. ToolsInUse itself unchanged. Also added read-only grounding pages for **Canvas Inputs** and **Python Functions** so every grounding kind has a UI page (Conversation Log exposes `AvailableCanvasInputs`/`AvailablePythonFunctions` + `Has*` flags; ChatWindow pushes `canvasInputs`/`pythonFunctions`). Grounding.svelte `view` union gained `'tools'|'canvas'|'python'`. 145 Core tests + svelte-check + slnx build all clean; live test pending.

**Follow-up 2 (2026-07-01) — referenced params placed at the arrow tip; NO move.** Requirement: the Rhino-referenced param should sit at the Component Transmitter's arrow tip (it feeds downstream), and the generated graph must not overlap it. First attempt MOVED the param to its reference-node layout slot during CompTx placement — user rejected it ("the move feels unnatural"). **Final approach:** the geometry tool places the param AT the arrow tip at creation and it never moves. `RhinoGeometryTool.PlacementAnchor(doc)` finds the (first) `ComponentTransmitter` and takes `transmitter.PlacementTarget ?? (Bounds.Right+50, vertical-mid)`; `AlignLeftMiddle` then lays the param out to read its real bounds and shifts the pivot so the param's LEFT-MIDDLE sits at `(tip.X + TipRightGap(20), tip.Y + row*gap)` — i.e. just right of the arrow head, arrow pointing straight into it (per user screenshot 2026-07-01). A param's pivot is not a fixed corner, hence the bounds-read-then-shift. Falls back to beside the tool node when no transmitter. `GhJsonBridge.RewireReferences` is **wire-only** (no pivot change); `ReferencePlan`/`ExtractReferences` use the `LiveById` dict (no pivot/offset). Builds clean, live test pending. (Superseded an earlier attempt that placed the param to the LEFT of the tip and, before that, one that MOVED the param during placement — both rejected.)

4. **`/t/<toolname>` prompt reference + purple `/`-command highlighting.** New pure `PromptToolResolver` (Core, mirrors `PromptClusterResolver`: `/t/X` → `the "X" tool`, +tests). ChatWindow `NormalizeRefs` chains cluster+tool resolvers on submit; pushes `availableTools`/`toolsWired` in state JSON (fed from `Conversation Log.AvailableToolNames`). Composer.svelte: the cluster menu was generalized to a `refMarker` ('c'|'t') menu (regex `/([ct])/`); tool matches show a WrenchIcon. Purple highlighting uses a **transparent-text textarea over a mirrored backdrop div** (`{@html highlighted}`, scroll-synced) — textareas can't style inline text. **Metric-parity is load-bearing (fixed 2026-07-01): the backdrop must wrap identically to the textarea or the caret drifts from the visible text on wrapped lines.** So `.slash-cmd` is COLOUR ONLY — an earlier `font-weight:500` widened the highlighted token in the backdrop, diverging the wrap and lagging the caret; also added `break-words` to the textarea to match the backdrop's word-breaking. Never give `.slash-cmd` a metric-affecting property (weight/size/letter-spacing/font-family). `.slash-cmd` + `--slash-command` token live in **app.css** (global), NOT a scoped `<style>` — Svelte scoped classes don't reach `{@html}` content. Highlights `/c/<known>`, `/t/<known>` (longest-first name match), and bare `/token` (image aliases). See [[picker-ghjson-serialization]], [[grounding-on-recorder]], [[document-units-grounding]].
